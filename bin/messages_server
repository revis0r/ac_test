#!/usr/bin/env ruby

require File.expand_path(File.join(File.dirname(__FILE__), '..', 'config', 'environment'))
require_relative '../lib/messages_server/command'

MessagesServer::Command.new(ARGV).daemonize